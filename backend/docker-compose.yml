services:
  bot:
    build: .
    container_name: guap-bot
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env.prod
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
